// Must start with Capital letter not small (Its Mandatory)
const Student = ({name, address}) => {
    return (
        <div>
            <h2>{name}</h2>
            <p>{address}</p>
        </div>
    )
}

export default Student